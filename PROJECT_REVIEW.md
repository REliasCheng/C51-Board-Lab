# Project Review

## Source inventory

| 项目 | 数量 |
| --- | ---: |
| 文件 | 572 |
| 总大小 | 137,120,782 bytes |
| Keil `.uvproj` | 29 |
| C/H/A51 源文件 | 83 |
| 图片 | 90 |
| PDF | 22 |
| Word | 3 |
| ZIP | 4 |

资料同时包含课程源码、教程、芯片手册、原理图、工具、驱动和构建产物。公开仓库只选取能够说明板级连接和软件实现的源码、工程配置与少量技术图片。

## Repository scope

`C51-Board-Lab` 关注 51 开天开发板上的资源分配和连接关系：

- P0 数据总线；
- P2 的 LED、I²C、74HC138 位选和 LCD 控制；
- P1 的矩阵键盘和电机控制；
- P3 的独立按键、UART、74HC595、DS1302、XPT2046 与 DS18B20；
- 数码管与点阵之间的 J24 选择；
- 从单文件程序到 `.c/.h` 模块，再到综合测试 BSP 的代码演进。

## Selected content

29 个 Keil 工程中迁移了 27 个有内容的单模块工程，并按 14 个技术主题合并展示；另从重复的综合测试压缩包中只提取源码和 `.uvproj`，形成第 15 个项目。

没有迁移：

- 空 `test` 工程；
- 命名与内容重复的旧数码管目录；
- Keil 安装包、注册工具、STC-ISP、CH340 驱动；
- PDF 教材、视频链接和芯片手册包；
- `.obj/.lst/.hex/.m51/.lnp/.uvopt`、`Objects/`、`Listings/` 和构建日志；
- 重复 ZIP 和完整课程交付目录。

## Source observations

- Keil 目标使用通用 `AT89C52`，课程定时代码按 11.0592 MHz 编写。
- DS1302 第二版的写保护寄存器常量为 `0x8`，与器件命令地址格式不一致；课程文件保留原样。
- RTC 示例启动时调用 `SetTime()`，会重新写入预设时间。
- UART 接收 ISR 内调用轮询 `TI` 的发送函数，适合简单回显，但会延长中断执行时间。
- AT24C02 示例实现了 ACK 时序，上层接口未系统处理 ACK 失败和写周期轮询。
- “红外遥控”工程只实现 INT0 边沿输入，没有包含教程中的完整 NEC 解码状态机。
- 多个单模块工程共享 P0、P2 和 P3.4~P3.7，综合使用前必须重新安排资源。

这些问题只记录在文档中；`course/` 下的源码未修改。
